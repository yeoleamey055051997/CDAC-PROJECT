CREATE TABLE owner (
	username VARCHAR(50) ,
	password VARCHAR(50) 
);

CREATE TABLE parking_details1 (
	id INT  NOT NULL,
	floar INT NOT NULL,
	vehicle_type VARCHAR(50) NOT NULL,
	capacity INT NOT NULL ,
	PRIMARY KEY (id) 
);

